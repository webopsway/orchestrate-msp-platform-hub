import React from 'react';
import { ServicePlaceholder } from '@/components/placeholder/ServicePlaceholder';

export const DynamicRoleTypeManager = () => {
  return (
    <ServicePlaceholder 
      title="Gestionnaire de rôles dynamiques" 
      description="Ce gestionnaire de rôles dynamiques est temporairement indisponible en raison de la migration de la base de données."
    />
  );
};